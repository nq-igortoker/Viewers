{
  "version": 2,
  "buildCommand": "cd platform/app && cross-env NODE_ENV=production APP_CONFIG=config/local_static.js yarn build",
  "outputDirectory": "platform/app/dist",
  "framework": null,
  "installCommand": "yarn install:frozen",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        },
        {
          "key": "X-XSS-Protection",
          "value": "1; mode=block"
        }
      ]
    }
  ]
}

